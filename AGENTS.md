# AGENTS.md

이 파일은 저장소 전체에 적용된다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 생기면 해당 디렉터리에서는 하위 문서의 지침을 함께 따른다.

## 프로젝트 목표

이 프로젝트는 PixiJS Canvas로 만드는 설치형 PWA 프로그래밍 학습 고양이 게임이다. 방은 통짜 이미지가 아니라 아이소메트릭 타일과 독립 오브젝트로 구성한다. 현재 우선순위는 PWA 게임을 완성하는 것이며 Windows/Linux 데스크톱 위젯 호스트는 후속 어댑터다.

작업 전 다음 문서 중 현재 작업과 관련된 부분을 읽는다.

- `README.md`: 실행 방법과 문서 진입점
- `docs/ARCHITECTURE.md`: 계층과 역할별 소유 경계
- `docs/CODE_GUIDE.md`: 주요 계약, JSON 흐름, 좌표계
- `docs/TEAM_WORKFLOW.md`: 브랜치, 검사, 리뷰 방식
- `public/assets/README.md`: 이미지 export와 카탈로그 규칙
- `docs/PDR.md`, `docs/adr/`: 제품 요구사항과 확정된 결정

## 고정 스캐폴딩

- `README.md`는 실행 방법과 문서 진입점만 담는 사용자 관리 고정 스캐폴딩이다.
- 기능 진행 상황, 테스트 수치, 구현 세부사항을 이유로 `README.md`를 수정하지 않는다.
- 사용자가 README 스캐폴딩 자체의 변경을 명시적으로 요청한 경우에만 수정한다.
- 기능별 설명은 `docs/`의 관련 문서와 코드 가까이에 기록한다.

## 작업자 수준과 교육 원칙

게임 시스템 담당자를 제외한 UI/HUD/화면 및 리소스 작업자는 프로그래밍 초보자로 간주한다. 요청자가 숙련도를 직접 밝히거나 대화에서 충분한 숙련도를 보인 경우 설명 깊이를 조정한다. 게임 시스템 담당자에게 기초 개념을 반복해서 설명하지 않는다.

초보 작업자를 도울 때 에이전트는 다음을 지킨다.

1. 작업을 시작할 때 무엇을 바꾸는지, 어떤 파일이 관련되는지, 왜 그 위치가 맞는지 짧게 설명한다.
2. 전문 용어를 처음 사용할 때 한 문장으로 뜻을 설명한다. 한 번 설명한 용어를 매번 다시 풀어 쓰지는 않는다.
3. 코드를 대신 작성하더라도 핵심 판단을 숨기지 않는다. 완료 시 “무엇이 바뀌었는가 / 왜 이렇게 했는가 / 어떻게 확인하는가”를 알려 준다.
4. 오류가 나면 긴 로그를 그대로 던지지 않는다. 가장 먼저 해결해야 할 오류, 원인, 수정 원리를 순서대로 설명한다.
5. 한 번에 너무 많은 대안을 제시하지 않는다. 현재 구조에 맞는 기본안을 먼저 추천하고 중요한 트레이드오프만 설명한다.
6. 작업자의 코드를 비난하거나 전면 재작성하지 않는다. 유지할 수 있는 부분과 고쳐야 하는 부분을 구분해 설명한다.
7. 학습을 이유로 작업을 미완성 상태로 넘기지 않는다. 요청이 구현이라면 안전한 범위에서 구현과 검증까지 완료한다.
8. 설명용 예시는 실제 프로젝트 타입과 명명 규칙을 사용하되, 복사할 필요가 없는 큰 코드 블록은 만들지 않는다.

초보자가 직접 이어서 작업해야 할 때는 마지막에 다음 행동을 하나 또는 두 개만 제시한다. 단순히 “더 공부하세요”라고 하지 말고 실행할 명령이나 열어 볼 파일을 정확히 알려 준다.

## 역할과 소유 경계

### 게임 시스템 담당

주 소유 경로:

- `src/domain/`
- `src/core/`
- 게임 규칙 관련 단위 테스트

책임:

- 상태, 보상, 정답 판정, 배치와 충돌 규칙
- `GameClient` 명령 및 JSON 결과 계약
- 로컬·원격 게임 클라이언트의 상태 전이

### UI/HUD/화면 담당

주 소유 경로:

- `src/game/`
- `src/content/ko.json`
- `src/style.css`
- Canvas 브라우저 스모크 테스트

책임:

- PixiJS 장면, HUD, 모달, 레이아웃, 애니메이션, 입력 표현
- `GameClient` 결과를 화면에 표현
- 사용자 메시지 키와 문구 관리

금지 사항:

- UI에서 재화, 정답, 최종 충돌 결과 또는 최초 보상을 권위 있게 판정하지 않는다. 배치 미리보기는 순수 규칙을 사용할 수 있지만 최종 결과는 `GameClient`가 결정한다.
- `LocalGameClient`, `localStorage`, 서버 구현을 화면 코드에서 직접 사용하지 않는다.
- Canvas 런타임에서 사용자에게 보이는 문장을 TypeScript에 하드코딩하지 않는다.

### 리소스 담당

주 소유 경로:

- `public/assets/`
- `public/assets/catalog.json`

책임:

- 스프라이트, 아이콘, 프레임, anchor와 bundle 등록
- 동일한 시점, 광원, 바닥 접점, 파일 규격 유지

금지 사항:

- 리소스 카탈로그에 가격, 점유 크기, 충돌, 보상, 희귀도 규칙을 넣지 않는다.
- TypeScript에 이미지 파일 경로를 하드코딩하도록 요청하지 않는다.
- 글자를 이미지에 굽지 않는다.

### 공동 연결부

다음 경로는 작은 통합 변경으로 다룬다.

- `src/app/`
- `src/main.ts`
- `src/services/`
- `src/pwa/`
- `vite.config.ts`
- `package.json`

다른 역할의 소유 파일을 수정해야 하면 이유와 영향 범위를 먼저 설명한다. 관련 없는 포맷 변경, 이름 변경, 파일 이동을 함께 하지 않는다.

## 코드 구조 규칙

허용되는 기본 의존 방향은 다음과 같다.

```text
app → game → GameClient 계약
app → LocalGameClient → domain
services → GameStateRepository 계약과 domain
game → domain의 읽기 전용 타입·순수 규칙
```

- `domain`과 `core`에서 PixiJS, DOM, `localStorage`를 사용하지 않는다.
- `game`은 `src/core/GameClient.ts`만 의존하고 다른 core 구현을 import하지 않는다.
- `GameState`에는 JSON으로 직렬화할 수 없는 객체를 넣지 않는다.
- 상태를 UI에서 직접 수정하지 않는다. `GameClient` 명령을 호출하고 새 snapshot을 받는다.
- 서버가 추가되어도 UI 계약이 유지되도록 성공과 실패를 판별 가능한 JSON 결과로 표현한다.

## Canvas와 PWA 규칙

- 방, HUD, 메뉴, 모달, 말풍선 등 사용자에게 보이는 게임 화면은 PixiJS Canvas 안에 만든다.
- HTML/DOM 오버레이로 게임 UI를 우회하지 않는다. PWA 셸에 필요한 DOM 진입점만 사용한다.
- 논리 좌표와 픽셀 좌표를 섞지 않는다. 변환은 `gridToScreen()`과 `screenToGrid()`를 사용한다.
- HUD는 방 카메라 컨테이너가 아니라 화면 좌표 UI 레이어에 둔다.
- PWA에서 먼저 동작해야 한다. OS별 위젯 코드를 게임 계층에 넣지 않는다.

## 메시지와 오류

- Canvas 런타임에서 사용자에게 보이는 모든 문구는 `src/content/ko.json`에 등록한다. 문서 제목과 PWA manifest 같은 플랫폼 메타데이터는 해당 설정 파일에서 관리한다.
- 게임 시스템은 완성된 사용자 문장이 아니라 `MessageId` 또는 기계 판독 가능한 `reason`을 반환한다.
- 동적 문구는 `{item}`, `{amount}` 같은 명명된 자리표시자를 사용한다.
- 예외, assertion, 개발자용 console 오류는 TypeScript에 둘 수 있다.
- 오류를 사용자 문구로 표시해야 하면 오류 코드를 메시지 키로 매핑한다. 내부 오류 문자열을 그대로 화면에 노출하지 않는다.

## 이미지 리소스

- 게임 장면의 런타임 이미지 경로는 `public/assets/catalog.json`에만 둔다. PWA 앱 아이콘 등 플랫폼 셸 리소스는 `vite.config.ts`에서 관리한다.
- 안정적인 asset ID를 코드와 저장 데이터에서 사용한다.
- 기본 투명 이미지는 WebP 또는 PNG를 사용하고 UI 아이콘은 필요에 따라 SVG를 허용한다.
- anchor는 0부터 1 사이의 정규화 좌표이며 바닥 접점을 기준으로 한다.
- 실제 리소스를 추가한 뒤 `npm run assets:check`를 실행한다.

## 코드 스타일

Biome 설정인 `biome.json`을 기준으로 한다.

- 모든 `if`, `else`, `for`, `while` 본문은 한 문장이더라도 중괄호를 사용한다.
- 중첩 삼항 연산자를 사용하지 않는다.
- `any`, non-null assertion(`!`), 매개변수 재할당으로 타입 문제를 우회하지 않는다.
- guard clause로 실패 조건을 먼저 처리하고 정상 흐름의 중첩을 줄인다.
- 함수는 하나의 분명한 책임을 갖도록 유지한다. 이름으로 설명 가능한 코드를 주석으로 반복하지 않는다.
- 기존 모듈에서 재사용할 수 있는 함수나 타입이 있으면 새 구현을 만들기 전에 사용 여부를 확인한다.

자동 정리:

```bash
npm run quality:fix
```

검사만 수행:

```bash
npm run quality:check
```

## TSDoc 규칙

변수, 단순 상수, 클래스 이름을 다시 설명하는 TSDoc은 작성하지 않는다.

다음 경우에 함수 또는 인터페이스 메서드 TSDoc을 작성한다.

- 다른 모듈에서 호출하는 공개 함수
- 좌표, 단위, 범위처럼 입력 전제가 있는 함수
- 저장, 구독, 상태 변경 같은 부작용이 있는 함수
- 실패 결과 또는 예외가 있는 함수
- 계층 사이의 중요한 계약

필요한 항목만 사용하되 한 줄 요약으로 끝내지 않는다.

- `@param`: 값의 의미, 좌표계, 허용 범위
- `@returns`: 성공·실패와 반환 객체의 보장
- `@throws`: 실제로 던질 수 있는 오류 조건
- `@remarks`: 부작용, 순수 함수 여부, 호출 순서, 의도적으로 하지 않는 일
- `@example`: 사용법이 이름만으로 분명하지 않을 때만 작은 예제

구현이 바뀌면 TSDoc과 `docs/CODE_GUIDE.md`의 JSON 예시를 같은 변경에서 갱신한다.

## 변경 절차

1. 요청과 관련 문서를 읽고 역할 소유 경계를 확인한다.
2. `rg`로 기존 타입, 함수, 메시지 키, asset ID를 먼저 찾는다.
3. 변경 범위를 작게 잡고 게임 규칙과 UI 표현을 한 함수에 섞지 않는다.
4. 게임 규칙 변경에는 성공과 실패 단위 테스트를 추가한다.
5. UI 흐름 변경에는 가능한 경우 브라우저 스모크를 추가하거나 갱신한다.
6. 아래 검사를 변경 위험에 맞게 실행한다.
7. 완료 메시지에 수정한 핵심 파일, 설계 이유, 통과한 검사를 적는다.

기본 검사:

```bash
npm run quality:check
npm run check
npm run structure:check
npm run assets:check
npm run test
npm run build
```

화면 흐름 변경:

```bash
npm run dev
# 다른 터미널에서
npm run smoke
```

PWA 셸, manifest 또는 service worker 변경:

```bash
npm run build
npm run preview
# 다른 터미널에서
npm run smoke:pwa
```

## 안전과 협업

- 작업 폴더의 기존 변경은 다른 작업자의 것일 수 있으므로 관련 없는 파일을 되돌리지 않는다.
- 요청하지 않은 대규모 리팩터링, 패키지 교체, 파일 이동을 하지 않는다.
- 삭제나 저장 형식 변경 전에는 대상과 마이그레이션 필요 여부를 확인한다.
- 테스트를 통과시키기 위해 규칙을 끄거나 타입을 약화하지 않는다.
- 검사 실패를 숨기지 않는다. 실패한 명령과 남은 원인을 짧고 정확하게 알려 준다.
- GitHub 권한, 배포, 외부 서비스 변경은 코드 수정과 별개의 권한이 필요하므로 요청 범위를 확인한다.

## 완료 응답 형식

초보 작업자에게는 다음 순서로 간결하게 전달한다.

1. 결과: 무엇이 동작하게 되었는가
2. 이해: 핵심 개념과 이 구조를 선택한 이유
3. 확인: 실행한 검사와 직접 확인하는 명령
4. 다음 행동: 작업자가 이어서 할 수 있는 한두 가지 구체적인 단계

숙련된 게임 시스템 담당자에게는 결과와 중요한 트레이드오프, 검증 결과를 중심으로 더 압축해서 전달한다.
