# When omitted, the PWA keeps using the local learning client.
VITE_CAT_GAME_API_BASE_URL=http://localhost:8000

# Optional in APP_ENV=local: the backend creates/reuses its development user.
# Required after production authentication is connected.
VITE_CAT_GAME_USER_PUBLIC_ID=
